name: test
channels:
  - pytorch
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
  - tqdm
  - pytorch-cpu

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - ProDy


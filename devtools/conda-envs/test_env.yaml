name: test
channels:
  - pytorch
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
  - tqdm
  - torch

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - ProDy


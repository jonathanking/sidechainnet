name: test
channels:
  - pytorch
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
  - tqdm

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - ProDy

